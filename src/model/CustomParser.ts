import {InsightDatasetKind} from "../controller/IInsightFacade";

export class CustomParser {
    public CustomParser() {
        let i = 0;
    }

    public addCustomParse(content: string, kind: InsightDatasetKind): Promise<string[]> {
    // TODO edit
        return  Promise.reject("Not implemented");
    }
}
