import {ISection} from "./Course";

export class PerformFilter {
    public performFilter(where: object, sections: ISection[]): ISection[] {
        let resultSections: ISection[] = [];
        return resultSections;
    }
}
